# HAMLOG branding page

A one-page static personal branding site for HAMLOG. It requires no build step and can be deployed directly to Vercel.

## Local preview

Run a static server in this directory, for example:

```powershell
python -m http.server 4173
```

Then open `http://localhost:4173`.
